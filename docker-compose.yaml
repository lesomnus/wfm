services:
  app:
    image: ghcr.io/lesomnus/wfm:${REVISION:-local}
    command: [greet, lesomnus]

    network_mode: host
    privileged: true

    volumes:
      - ./wfm.yaml:/wfm.yaml:ro

    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 64M
        reservations:
          cpus: "2"
          memory: 32M
